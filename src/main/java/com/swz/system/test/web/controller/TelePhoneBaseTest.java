package com.swz.system.test.web.controller;

import com.swz.data.vo.AlarmDevice;
import com.swz.data.vo.PhoneStream;

/**
 * 基本的电话语音测试
 * 
 * @author charsboll
 *
 */
public class TelePhoneBaseTest {
	// 先创建一个设备
	private boolean needalarm;
	private AlarmDevice alarmDevice;
	private PhoneStream phoneStream;

	public void test() {
		// 不用报警了
		if (!needalarm) {
			// 跳过
		} else {

		}
	}
}
